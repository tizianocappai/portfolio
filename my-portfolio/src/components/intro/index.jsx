import React from "react";

import './styles.scss';

const Intro = ()=> {
    return (
        <section className={"intro-section"}>
            hello
        </section>
    )
}

export default React.memo(Intro);
